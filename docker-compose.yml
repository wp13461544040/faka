version: '3.8'

services:
  faka:
    build: .
    container_name: faka_system
    ports:
      - "3019:3019"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
